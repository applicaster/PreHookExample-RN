import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  closeButton: {
    height: 38,
    position: 'absolute',
    right: 15,
    top: 15,
    width: 38,
    zIndex: 3,
  },
  videoContainer: {
    marginTop: '33%',
  },
});
