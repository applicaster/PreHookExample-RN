import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  eventCaption: {
    fontSize: 18,
    lineHeight: 22,
    marginHorizontal: 12,
    marginTop: 17,
    marginBottom: 5,
  },
});
