import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  videoTitle: {
    fontSize: 23,
    fontWeight: '800',
    paddingHorizontal: 13,
    marginTop: 12,
    marginBottom: 20,
    color: '#000000',
  },
});
