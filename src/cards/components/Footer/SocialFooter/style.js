import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  buttonBar: {
    flexDirection: 'row',
    alignItems: 'center',
    height: 30,
    paddingHorizontal: 9,
    marginBottom: 7,
  },
  shareButtonContainer: {
    position: 'absolute',
    bottom: 3,
    right: 7,
  },
});
