import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  feedListContent: {
    backgroundColor: 'transparent',
    paddingTop: 10,
  },
  feedList: {
    backgroundColor: 'transparent',
    paddingBottom: 5,
  },
});
