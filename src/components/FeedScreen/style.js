import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  feedListContent: {
    backgroundColor: 'transparent',
    paddingTop: 5,
  },
  feedList: {
    backgroundColor: 'transparent',
    paddingBottom: 5,
  },
});
