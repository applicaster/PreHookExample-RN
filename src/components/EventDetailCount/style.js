import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  eventDetailCount: {
    flexDirection: 'row',
    alignItems: 'center',
    marginRight: 5,
  },
  count: {
    fontSize: 12,
    marginRight: 3,
  },
  label: {
    fontSize: 12,
  },
});
