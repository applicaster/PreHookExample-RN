import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  eventCaption: {
    fontSize: 14,
    marginHorizontal: 12,
    marginTop: 17,
    marginBottom: 5,
    lineHeight: 18,
  },
});
