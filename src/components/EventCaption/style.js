import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  eventCaption: {
    fontSize: 14,
    paddingHorizontal: 12,
    paddingTop: 17,
    paddingBottom: 5,
    lineHeight: 18,
  },
});
