import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  eventContainer: {
    borderRadius: 7,
    marginBottom: 10,
    marginHorizontal: 10,
    overflow: 'hidden',
    shadowColor: 'black',
    shadowOpacity: 0.4,
    shadowRadius: 5,
    shadowOffset: {
      width: 4,
      height: 5,
    },
  },
});
