import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  eventContainer: {
    borderRadius: 5,
    marginBottom: 5,
    marginHorizontal: 5,
    overflow: 'hidden',
  },
});
