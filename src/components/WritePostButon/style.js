import { StyleSheet } from 'react-native';

const buttonSize = 60;
export const styles = StyleSheet.create({
  writePostButton: {
    position: 'absolute',
    height: buttonSize,
    width: buttonSize,
    bottom: 15,
    right: 10,
  },
  writePostPencil: {
    height: 35,
    width: 30,
    bottom: 30,
    right: 24,
    position: 'absolute',
  },
});
