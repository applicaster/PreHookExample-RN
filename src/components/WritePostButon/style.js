import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  writePostButton: {
    height: 60,
    width: 60,
    bottom: 15,
    right: 15,
    position: 'absolute',
    zIndex: 2,
  },
});
