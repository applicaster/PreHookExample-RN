machine:
  node:
    version: 8.1.4
  post:
    - npm i -g npm

test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit
    - npm run test-reporter
    - cp test-results.xml $CIRCLE_TEST_REPORTS/junit

deploy:
  production:
    branch: master